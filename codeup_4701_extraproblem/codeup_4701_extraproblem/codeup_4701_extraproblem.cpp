#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

int n;
vector<ll> arr;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	cin >> n;
	arr.resize(n);
	for (int i = 0; i < n; ++i)
	{
		cin >> arr[i];
	}
}